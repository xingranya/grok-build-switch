# 治理面决议

## 指令面

| 指令面 | 状态 | 角色 | 说明 |
|:---|:---|:---|:---|
| `AGENTS.md` | 未创建 | 共享代理规则 | 用户在会话中提供了等价的始终生效规则；本次不擅自复制或创建新权威源 |
| `CLAUDE.md` | 不使用 | Claude Code 专用规则 | 仓库原先不存在，当前任务不需要新增 |
| `.cursor/rules/` | 不存在 | Cursor 规则 | 不触碰 |
| `.windsurf/` | 不存在 | Windsurf 规则 | 不触碰 |
| `.clinerules*` | 不存在 | Cline 规则 | 不触碰 |
| `.codex/` | 不存在 | Codex 项目配置 | 不触碰 |
| `.codegraph/` | 已初始化 | AST 代码索引 | 按用户规则建立，不作为指令或记忆源 |

## 记忆面

| 字段 | 值 |
|:---|:---|
| 可写原生项目记忆 | 否 |
| 已解析记忆面 | 不可用 |
| 仓库回退文件已获批准 | 否 |
| 处理方式 | 不创建仓库内记忆文件；稳定决策写入进度与最终归档 |

## 当前权威来源

1. 用户在当前会话提供的始终生效项目规则。
2. `docs/progress/MASTER.md`：规范驱动执行状态唯一真源。
3. `docs/analysis/` 与 `docs/plan/`：本次适配的分析和范围真源。
4. 源码、自动化测试和最终构建产物：行为真源。
