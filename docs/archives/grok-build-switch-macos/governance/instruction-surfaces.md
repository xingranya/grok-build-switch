# 指令面归档快照

本次任务没有创建仓库级 `AGENTS.md` 或 `CLAUDE.md`。执行期间的共享规则由用户在会话中直接提供，核心约束如下：

- 始终使用简体中文与用户沟通。
- 结构查询优先使用 CodeGraph，文本查询使用 ripgrep。
- 所有终端命令通过 `rtk` 执行。
- 不执行非必要的启动类操作。
- 注释、文档和函数级说明使用简体中文。
- 保持项目既有风格，行为改动必须有相应测试。

详细治理决议保存在 [governance.md](../progress/governance.md)。
